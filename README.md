***

# Django Event Manager API

REST API сервис для управления мероприятиями, разработанный на Django 6 и Django REST Framework. Поддерживает работу с геоданными (PostGIS), асинхронные задачи (Celery), автоматический сбор погоды и рассылку уведомлений.

Проект подходит как для локального запуска на Windows, так и для развертывания в Docker-контейнерах.

## Основные возможности

*   **Мероприятия и Площадки:** Полный CRUD для управления событиями. Поддержка гео-координат (PointField) для площадок.
*   **Автоматическая погода:** Интеграция с Open-Meteo. При публикации события или смене локации система сама подгружает прогноз погоды.
*   **Асинхронные задачи:** Отложенная публикация событий, фоновое обновление погоды, email-рассылки через Celery + Redis.
*   **Медиа:** Загрузка галереи изображений с автоматической генерацией превью.
*   **Импорт/Экспорт:** Поддержка массовой загрузки и выгрузки событий через XLSX файлы.
*   **Гибкие настройки:** Управление публичным доступом и email-провайдерами через переменные окружения.
*   **Документация:** Встроенная Swagger/OpenAPI схема.

***

## 🛠 Технический стек

*   **Python 3.12**
*   **Django 6.0 + DRF**
*   **PostgreSQL + PostGIS** (Гео-данные)
*   **Celery + Redis** (Очереди задач)
*   **Docker & Docker Compose**
*   **Pytest** (Тестирование)

***

## 🚀 Запуск через Docker (Рекомендуемый)

Самый простой способ запустить проект со всеми зависимостями (БД, Redis, Worker). Миграции применяются автоматически при первом старте контейнера.

### 0. Скачивание проекта
```bash
mkdir event_manager
git init
git remote add origin https://github.com/mylittlepray/EventManager.git
git pull origin main
```

### 1. Подготовка окружения
Создайте файл `.env` в корне проекта (можно скопировать `.env.example`).
**Ключевые настройки:**
```ini
DJANGO_SECRET_KEY='your-django-key' # настройте секретный ключ Django после установки (см. далее)
DEBUG=1 # Режим отладки 
ALLOWED_HOSTS=localhost,127.0.0.1,web

POSTGRES_DB=event_manager_db
POSTGRES_USER=postgres
POSTGRES_PASSWORD=my_strong_password
POSTGRES_HOST=db # 127.0.0.1 для винды
POSTGRES_PORT=5432

CELERY_TIMEZONE=UTC
CELERY_BROKER_URL=redis://redis:6379/0
CELERY_RESULT_BACKEND=redis://redis:6379/0

DEFAULT_FROM_EMAIL = 'noreply@yourdomain.com'
MAILERSEND_HOST = 'connect.smtp.com'
MAILERSEND_USER = 'your@mail.com'
MAILERSEND_KEY = 'your_Key'
```

### 2. Запуск контейнеров
```bash
docker-compose up -d --build
```
Эта команда соберет образы, запустит базу данных, Redis, веб-сервер и Celery воркеры.
Миграции базы данных применяются автоматически при старте веб-контейнера.

### 2.1. **Генерация надежного Secret Key**

Для безопасности (особенно перед деплоем) нужно сгенерировать уникальный `DJANGO_SECRET_KEY`. Вы можете сделать это через запущенный контейнер:

### 2.1.1. **Сгенерируйте ключ командой:**
   ```bash
   docker-compose exec web python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
   ```

### 2.1.2. **Обновите `.env`:**
   Скопируйте полученную строку и замените значение переменной в вашем файле `.env`:
   ```ini
   DJANGO_SECRET_KEY='вставьте-скопированный-ключ-сюда'
   ```

### 2.1.3. **Перезапустите контейнеры:**
   Чтобы новый ключ применился, нужно пересоздать контейнеры:
   ```bash
   docker-compose up -d --build
   ```

### 3. Создание суперпользователя
Для доступа в админ-панель создайте администратора:
```bash
docker-compose exec web python manage.py createsuperuser
```

### 4. Наполнение тестовыми данными (Seed)
В проекте есть команда для генерации тестовых площадок и мероприятий:
```bash
docker-compose exec web python manage.py seed_data --venues 15 --events 60
```

### 5. Запуск тестов
Вы можете запустить полный набор тестов (Pytest) прямо внутри контейнера:
```bash
docker-compose exec web pytest
```

***

## 💻 Запуск локально (Windows)

Для запуска без Docker вам потребуется установленный PostgreSQL (с расширением PostGIS) и Redis.

### 1. Установка Geo-библиотек (OSGeo4W)
GeoDjango требует системные библиотеки GDAL и GEOS.
1. Скачайте установщик [OSGeo4W](https://trac.osgeo.org/osgeo4w/).
2. Установите в `C:\OSGeo4W` (или аналогичный путь).
3. В файле `config/settings.py` укажите пути к DLL (раскомментируйте или добавьте):
```python
if os.name == 'nt':
    GDAL_LIBRARY_PATH = r"C:\Path\To\OSGeo4W\bin\gdal312.dll"
    GEOS_LIBRARY_PATH = r"C:\Path\To\OSGeo4W\bin\geos_c.dll"
```

### 2. Настройка окружения
Создайте файл `.env` и укажите настройки для локального PostgreSQL и Redis:
```ini
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
CELERY_BROKER_URL=redis://localhost:6379/0
```

### 3. Установка зависимостей
Создайте и активируйте виртуальное окружение, затем установите пакеты:
```bash
python -m venv .venv
.venv\Scripts\activate
pip install -r requirements.txt
```

### 4. Запуск проекта
```bash
# Применение миграций
python manage.py migrate

# Создание суперпользователя
python manage.py createsuperuser

# Запуск сервера разработки
python manage.py runserver

# Запуск Celery (в отдельном терминале, требует совместимости или gevent)
celery -A config worker --loglevel=info -P gevent
```

***

## ⚙️ Конфигурация (.env и settings.py)

Вы можете гибко настраивать поведение приложения через переменные окружения и настройки.

### Email уведомления
В режиме `DEBUG=True` письма выводятся в консоль. Для отправки реальных писем в продакшене (SMTP), настройте переменные в `.env`:
```ini
MAILERSEND_HOST=smtp.mailersend.net
MAILERSEND_USER=your-user
MAILERSEND_KEY=your-api-key
DEFAULT_FROM_EMAIL=[email protected]
```

### Доступ к площадкам
В `settings.py` есть настройка `VENUES_PUBLIC_READ_ACCESS`.
*   `True`: Список площадок доступен для чтения всем (даже анонимным пользователям).
*   `False`: Доступ только у администраторов.
Удобно для переключения режима работы API без изменения кода.

***

## 📚 Документация и Интерфейсы

После запуска проекта доступны следующие адреса:

*   **Swagger API Docs:** [http://localhost:8000/api/docs/](http://localhost:8000/api/docs/) — Интерактивная документация всех эндпоинтов.
*   **ReDoc:** [http://localhost:8000/api/schema/](http://localhost:8000/api/schema/) — Альтернативная схема API.
*   **Админ-панель:** [http://localhost:8000/admin/](http://localhost:8000/admin/)
*   **Frontend (Demo):** [http://localhost:8000/](http://localhost:8000/) — Простой фронт
*   **Схема БД:** В файле Scheme for EventManager.png в корне проекта

***

## 🧩 Структура проекта

*   `events/` — Основная логика: модели событий, изображения, задачи Celery.
    *   `management/commands/seed_data.py` — Скрипт генерации тестовых данных.
*   `venues/` — Гео-площадки, работа с координатами.
*   `weather/` — Сервис интеграции с погодным API.
*   `core/` — Базовые настройки и общие утилиты.
*   `tests/` — Папка с тестами.
*   `templates/` — Шаблоны.
*   `static/` — Статические файлы.
*   `media` — Медиафайлы.

***